var fruit = 'orange';
//console.log(fruit.length);

if (fruit.length > 5) { console.log('The fruit name has more than five characters.')
    } 
    else {console.log('The fruit name has five characters or less.')
};

//when comparing a var with a string value, you need to add a method to the var in order for any comparison to be made.  The computer does
//not know how to evaluate string otherwise.